spring:
  application:
    name: rft-gateway-service
  boot:
    admin:
      client:
        url: http://localhost:8888

server:
  port: 8089

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      default-zone: https://localhost:8761/eureka/

ribbon:
  eureka:
    enabled: true